# DeepSeek 模型家族 Profile 说明

<!-- 由 scripts/generate_test_docs.py 从 Model Profile Database 与测试扩展生成，请勿手工维护表格。 -->

覆盖思考档位、采样、JSON、停止词、logprobs、工具调用以及阿里云/动态聚合差异。

本文档回答三个问题：这个家族有哪些模型身份、在不同 route/API Form 下使用哪份测试契约、每个 profile 实际发送什么并检查什么。

## 在界面中使用本手册

![文字参数测试界面示意](../assets/ui/parameter-testing-console.svg)

先在界面按 Provider → Model → Route Profile → API Form → Reference Contract 选择组合，再用下文表格确认本次会运行哪些 Test Case。

## 先理解判读规则

- `应支持`：期望 HTTP 2xx，且响应结构、内容语义、usage、returned-model 均通过校验。
- `应拒绝`：期望明确的 400/422；若仍返回 2xx，记为 `unexpected_acceptance`。
- `按模型/route 变化`：同一 profile 对家族内不同模型或 route 的期望不同，运行前以控制台展开的 model profile 为准。
- 动态聚合 route 即使全部通过，也只证明 adapter 兼容，不能证明物理上游或原厂合同。

## 模型与 alias

| 规范模型 | 显式 alias |
|---|---|
| `deepseek-v4-flash` | — |
| `deepseek-v4-flash-0731` | — |
| `deepseek-v4-pro` | — |
| `deepseek-v4-pro-0813` | — |

## Route 与 API Form

| Route Profile | API Form | 内部 transport | 已注册模型数 | Reference Contract |
|---|---|---|---:|---|
| `aliyun_maas` | `openai_chat_completions` | `chat_completions` | 3 | `aliyun_deepseek_v4_flash_0731_openai_compat`<br>`aliyun_deepseek_v4_openai_compat` |
| `vendor_direct` | `openai_chat_completions` | `chat_completions` | 2 | `deepseek_chat`<br>`deepseek_v4_pro_0813_chat` |
| `vendor_direct` | `anthropic_messages` | `claude_messages` | 1 | `deepseek_v4_pro_0813_anthropic` |
| `vendor_direct` | `openai_responses` | `openai_responses` | 1 | `deepseek_v4_pro_0813_responses` |
| `vendor_direct` | `openai_fim_completions_beta` | `fim_completions` | 1 | `deepseek_v4_pro_0813_fim_beta` |

## Reference Contract

| Reference Contract | 说明 | Route / API Form | 认证范围 | Test Case 数 | 官方资料 |
|---|---|---|---|---:|---|
| `aliyun_deepseek_v4_flash_0731_openai_compat` | Alibaba Cloud DeepSeek V4 Flash 0731 OpenAI-compatible Chat | `aliyun_maas` / `openai_chat_completions` | `raw_route_contract` | 13 | [资料1](https://help.aliyun.com/zh/model-studio/deepseek-v4-flash) [资料2](https://help.aliyun.com/zh/model-studio/deepseek-api) |
| `aliyun_deepseek_v4_openai_compat` | Alibaba Cloud Model Studio DeepSeek V4 OpenAI-compatible Chat | `aliyun_maas` / `openai_chat_completions` | `raw_route_contract` | 13 | [资料1](https://help.aliyun.com/zh/model-studio/qwen-api-via-openai-chat-completions) [资料2](https://help.aliyun.com/zh/model-studio/deepseek-api) [资料3](https://help.aliyun.com/zh/model-studio/text-generation-model) |
| `deepseek_chat` | DeepSeek Chat Completions | `vendor_direct` / `openai_chat_completions` | `raw_route_contract` | 18 | [资料1](https://api-docs.deepseek.com/api/create-chat-completion) [资料2](https://api-docs.deepseek.com/guides/json_mode/) [资料3](https://api-docs.deepseek.com/guides/thinking_mode) |
| `deepseek_v4_pro_0813_anthropic` | DeepSeek V4 Pro 0813 Anthropic Messages compatibility | `vendor_direct` / `anthropic_messages` | `raw_route_contract` | 23 | [资料1](https://api-docs.deepseek.com/updates/) [资料2](https://api-docs.deepseek.com/guides/anthropic_api) |
| `deepseek_v4_pro_0813_chat` | DeepSeek V4 Pro 0813 Chat Completions | `vendor_direct` / `openai_chat_completions` | `raw_route_contract` | 18 | [资料1](https://api-docs.deepseek.com/updates/) [资料2](https://api-docs.deepseek.com/api/create-chat-completion) [资料3](https://api-docs.deepseek.com/guides/thinking_mode) |
| `deepseek_v4_pro_0813_fim_beta` | DeepSeek V4 Pro 0813 FIM Completion Beta | `vendor_direct` / `openai_fim_completions_beta` | `raw_route_contract` | 15 | [资料1](https://api-docs.deepseek.com/updates/) [资料2](https://api-docs.deepseek.com/api/create-completion) |
| `deepseek_v4_pro_0813_responses` | DeepSeek V4 Pro 0813 Responses API | `vendor_direct` / `openai_responses` | `raw_route_contract` | 42 | [资料1](https://api-docs.deepseek.com/updates/) [资料2](https://api-docs.deepseek.com/guides/responses_api) [资料3](https://api-docs.deepseek.com/api/create-response) |

## 全部参数 Profile

| Profile | 类别 | 具体测试目的 | 关键请求设置 | 期望 | 通过时还要检查 |
|---|---|---|---|---|---|
| `aliyun_stream` | 流式 | 验证 SSE 流式响应、结束标记和返回文本能够完整解析。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=true`<br>`max_completion_tokens=128` | 应支持 | 检查 chunk 结构、结束标记、文本拼接与 usage 末块。 |
| `aliyun_stream_usage` | 流式 | 验证 SSE 分块可解析，并在结束前得到独立、算术一致的 usage 信息。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=true`<br>`max_completion_tokens=128`<br>`stream_options.include_usage=true` | 应支持 | 检查 chunk 结构、结束标记、文本拼接与 usage 末块。 |
| `aliyun_max_completion_tokens` | 基础能力 | 验证 `max_completion_tokens`、`messages` 的请求兼容性以及对应响应字段是否正常。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=false`<br>`max_completion_tokens=128` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `aliyun_enable_thinking` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `enable_thinking`、`max_completion_tokens`、`messages`。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=false`<br>`enable_thinking=true`<br>`max_completion_tokens=256` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `aliyun_disable_thinking` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `enable_thinking`、`max_completion_tokens`、`messages`。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=false`<br>`enable_thinking=false`<br>`max_completion_tokens=128` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `aliyun_reasoning_high` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `max_completion_tokens`、`messages`、`reasoning_effort`。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=false`<br>`reasoning_effort="high"`<br>`max_completion_tokens=256` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `aliyun_reasoning_max` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `max_completion_tokens`、`messages`、`reasoning_effort`。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=false`<br>`reasoning_effort="max"`<br>`max_completion_tokens=256` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `aliyun_temperature` | 采样 | 验证采样参数 `messages`、`temperature` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=false`<br>`temperature=0.7`<br>`max_completion_tokens=128` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `aliyun_top_p` | 采样 | 验证采样参数 `messages`、`top_p` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=false`<br>`top_p=0.8`<br>`max_completion_tokens=128` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `aliyun_stop` | 基础能力 | 验证停止序列参数 `messages`、`stop` 会影响结束位置或按契约被拒绝。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=false`<br>`stop=["END"]`<br>`max_completion_tokens=128` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `aliyun_json_object` | 结构化输出 | 验证结构化输出参数 `messages`、`response_format`，并确认最终内容是可解析且符合约束的 JSON。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=false`<br>`enable_thinking=false`<br>`response_format.type="json_object"`<br>`max_completion_tokens=256` | 应支持 | 内容必须能解析为 JSON；有 schema 时还要满足 schema。 |
| `aliyun_tools` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=false`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`multi_turn=true`<br>`max_completion_tokens=512` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `aliyun_tool_choice_auto` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`aliyun_deepseek_v4_flash_0731_openai_compat`、`aliyun_deepseek_v4_openai_compat` | `stream=false`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`multi_turn=true`<br>`max_completion_tokens=512`<br>`tool_choice="auto"` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `basic_stream` | 流式 | 验证 SSE 流式响应、结束标记和返回文本能够完整解析。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=true`<br>`thinking.type="disabled"` | 应支持 | 检查 chunk 结构、结束标记、文本拼接与 usage 末块。 |
| `stream_with_usage` | 流式 | 验证 SSE 分块可解析，并在结束前得到独立、算术一致的 usage 信息。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=true`<br>`thinking.type="disabled"`<br>`stream_options.include_usage=true` | 应支持 | 检查 chunk 结构、结束标记、文本拼接与 usage 末块。 |
| `thinking_low` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `messages`、`reasoning_effort`、`response.reasoning_content`、`thinking`。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="enabled"`<br>`reasoning_effort="low"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `thinking_enabled` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `messages`、`reasoning_effort`、`response.reasoning_content`、`thinking`。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="enabled"`<br>`reasoning_effort="high"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `thinking_max` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `messages`、`reasoning_effort`、`response.reasoning_content`。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="enabled"`<br>`reasoning_effort="max"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `thinking_disabled` | 负向/边界 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `messages`、`response.reasoning_content`、`thinking`。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="disabled"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek_max_tokens` | 基础能力 | 验证输出 token 上限字段 `max_tokens`、`messages` 使用当前 API Form 的正确名称和位置。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="disabled"`<br>`max_tokens=64` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek_user_id` | 基础能力 | 验证 `messages`、`user_id` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="disabled"`<br>`user_id="loadtest_deepseek_param_user"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `sampling_non_thinking` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `messages`、`temperature`、`top_p`。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="disabled"`<br>`temperature=0.7`<br>`top_p=0.9` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek_json_output_256` | 结构化输出 | 验证结构化输出参数 `messages`、`response_format`，并确认最终内容是可解析且符合约束的 JSON。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="disabled"`<br>`response_format.type="json_object"`<br>`max_tokens=256` | 应支持 | 内容必须能解析为 JSON；有 schema 时还要满足 schema。 |
| `stop_sequences` | 基础能力 | 验证停止序列参数 `messages`、`stop` 会影响结束位置或按契约被拒绝。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="disabled"`<br>`stop=["\n\n","END"]` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `logprobs` | 基础能力 | 验证 `logprobs`、`messages`、`top_logprobs` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="disabled"`<br>`logprobs=true`<br>`top_logprobs=5` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `tool_calls` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="disabled"`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`multi_turn=true` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `tool_choice_required` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="disabled"`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`multi_turn=true`<br>`tool_choice="required"` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek_tool_strict` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="disabled"`<br>`tools_fixture="fixtures/tools_weather_strict.json"`<br>`tool_choice="required"`<br>`multi_turn=true` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `tool_calls_thinking` | 工具调用 | 验证推理模式下的结构化工具调用，并确认历史推理字段在 follow-up 中原样保留。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="enabled"`<br>`reasoning_effort="high"`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`multi_turn=true`<br>`pass_reasoning_content=true` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek_frequency_penalty` | 采样 | 验证采样参数 `frequency_penalty`、`messages` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="disabled"`<br>`frequency_penalty=0.2`<br>`send_deprecated=true` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek_presence_penalty` | 采样 | 验证采样参数 `messages`、`presence_penalty` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`deepseek_chat`、`deepseek_v4_pro_0813_chat` | `stream=false`<br>`thinking.type="disabled"`<br>`presence_penalty=0.2`<br>`send_deprecated=true` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_basic` | 基础能力 | 验证 `max_tokens`、`messages`、`response.content`、`response.usage` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_stream` | 流式 | 验证 SSE 流式响应、结束标记和返回文本能够完整解析。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=true`<br>`max_tokens=32` | 应支持 | 检查 chunk 结构、结束标记、文本拼接与 usage 末块。 |
| `deepseek0813_anthropic_system` | 基础能力 | 验证系统指令字段 `max_tokens`、`messages`、`response.content`、`response.usage`、`system` 的协议位置和实际响应语义。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`system="You are a concise DeepSeek Anthropic API compatibility t...` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_stop_sequences` | 基础能力 | 验证停止序列参数 `max_tokens`、`messages`、`response.content`、`response.usage`、`stop_sequences` 会影响结束位置或按契约被拒绝。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`stop_sequences=["END"]` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_temperature` | 采样 | 验证采样参数 `max_tokens`、`messages`、`response.content`、`response.usage`、`temperature` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`temperature=2` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_reject_temperature` | 负向/边界 | 负向探针：发送文档不允许的 `max_tokens`、`messages`、`response.content`、`response.usage`、`temperature`，确认网关明确拒绝而不是静默吞掉。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`preserve_rejected_params=true`<br>`temperature=2.1` | 应拒绝 | 应得到明确 400/422；若 2xx 则是 unexpected_acceptance。 |
| `deepseek0813_anthropic_top_p` | 采样 | 验证采样参数 `max_tokens`、`messages`、`response.content`、`response.usage`、`top_p` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`top_p=1` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_thinking_budget_ignored` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `max_tokens`、`messages`、`response.content`、`response.usage`、`thinking`、`thinking.budget_tokens`。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`thinking.type="enabled"`<br>`thinking.budget_tokens=64` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_anthropic_effort_high` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `max_tokens`、`messages`、`output_config.effort`、`response.content`、`response.usage`、`thinking`。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=64`<br>`thinking.type="enabled"`<br>`output_config.effort="high"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_anthropic_effort_max` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `max_tokens`、`messages`、`output_config.effort`、`response.content`、`response.usage`、`thinking`。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=64`<br>`thinking.type="enabled"`<br>`output_config.effort="max"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_anthropic_tools_auto` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=64`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`tool_choice="auto"`<br>`multi_turn=true` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek0813_anthropic_tools_any` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=64`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`tool_choice="any"`<br>`multi_turn=true` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek0813_anthropic_tools_named` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=64`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`tool_choice="get_weather"`<br>`multi_turn=true` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek0813_anthropic_tool_choice_none` | 负向/边界 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=64`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`tool_choice="none"`<br>`multi_turn=false` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek0813_anthropic_metadata_user_id` | 基础能力 | 验证 route 专属元数据 `max_tokens`、`messages`、`metadata.user_id`、`response.content`、`response.usage` 放在正确的 body 或 header 位置。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`metadata.user_id="loadtest_deepseek_anthropic_user"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_container_accepted_ignored` | 基础能力 | 验证 `container`、`max_tokens`、`messages`、`response.content`、`response.usage` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`container="deepseek-anthropic-ignored-container"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_mcp_servers_accepted_ignored` | 基础能力 | 验证 `max_tokens`、`mcp_servers`、`messages`、`response.content`、`response.usage` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`mcp_servers=[]` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_metadata_other_accepted_ignored` | 基础能力 | 验证 route 专属元数据 `max_tokens`、`messages`、`metadata.*`、`response.content`、`response.usage` 放在正确的 body 或 header 位置。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`metadata.compatibility_probe="ignored"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_service_tier_accepted_ignored` | Route 元数据 | 验证 route 专属元数据 `max_tokens`、`messages`、`response.content`、`response.usage`、`service_tier` 放在正确的 body 或 header 位置。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`service_tier="auto"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_top_k_accepted_ignored` | 采样 | 验证采样参数 `max_tokens`、`messages`、`response.content`、`response.usage`、`top_k` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`top_k=40` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_output_config_other_accepted_ignored` | 基础能力 | 验证 `max_tokens`、`messages`、`output_config.*`、`response.content`、`response.usage` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=64`<br>`thinking.type="enabled"`<br>`output_config.effort="high"`<br>`output_config.verbosity="low"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_anthropic_tools_cache_control_accepted_ignored` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`tools=[1 items]`<br>`tool_choice="none"` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek0813_anthropic_disable_parallel_tool_use_accepted_ignored` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_v4_pro_0813_anthropic` | `transport="claude_messages"`<br>`stream=false`<br>`max_tokens=32`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`tool_choice.type="auto"`<br>`tool_choice.disable_parallel_tool_use=true` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek0813_fim_basic` | 基础能力 | 验证 `max_tokens`、`prompt`、`response.choices[].text` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_fim_suffix` | 基础能力 | 验证 `max_tokens`、`prompt`、`response.choices[].text`、`suffix` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16`<br>`suffix="\n# end"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_fim_echo` | 基础能力 | 验证 `echo`、`max_tokens`、`prompt`、`response.choices[].text` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16`<br>`echo=true` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_fim_logprobs` | 基础能力 | 验证 `logprobs`、`max_tokens`、`prompt`、`response.choices[].logprobs`、`response.choices[].text` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16`<br>`logprobs=20` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_fim_reject_logprobs` | 负向/边界 | 负向探针：发送文档不允许的 `logprobs`、`max_tokens`、`prompt`、`response.choices[].text`，确认网关明确拒绝而不是静默吞掉。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16`<br>`preserve_rejected_params=true`<br>`logprobs=21` | 应拒绝 | 应得到明确 400/422；若 2xx 则是 unexpected_acceptance。 |
| `deepseek0813_fim_stop` | 基础能力 | 验证停止序列参数 `max_tokens`、`prompt`、`response.choices[].text`、`stop` 会影响结束位置或按契约被拒绝。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16`<br>`stop=[16 items]` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_fim_reject_stop_count` | 负向/边界 | 负向探针：发送文档不允许的 `max_tokens`、`prompt`、`response.choices[].text`、`stop`，确认网关明确拒绝而不是静默吞掉。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16`<br>`preserve_rejected_params=true`<br>`stop=[17 items]` | 应拒绝 | 应得到明确 400/422；若 2xx 则是 unexpected_acceptance。 |
| `deepseek0813_fim_temperature` | 采样 | 验证采样参数 `max_tokens`、`prompt`、`response.choices[].text`、`temperature` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16`<br>`temperature=2` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_fim_reject_temperature` | 负向/边界 | 负向探针：发送文档不允许的 `max_tokens`、`prompt`、`response.choices[].text`、`temperature`，确认网关明确拒绝而不是静默吞掉。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16`<br>`preserve_rejected_params=true`<br>`temperature=2.1` | 应拒绝 | 应得到明确 400/422；若 2xx 则是 unexpected_acceptance。 |
| `deepseek0813_fim_top_p` | 采样 | 验证采样参数 `max_tokens`、`prompt`、`response.choices[].text`、`top_p` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16`<br>`top_p=1` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_fim_reject_top_p` | 负向/边界 | 负向探针：发送文档不允许的 `max_tokens`、`prompt`、`response.choices[].text`、`top_p`，确认网关明确拒绝而不是静默吞掉。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16`<br>`preserve_rejected_params=true`<br>`top_p=1.1` | 应拒绝 | 应得到明确 400/422；若 2xx 则是 unexpected_acceptance。 |
| `deepseek0813_fim_stream` | 流式 | 验证 SSE 流式响应、结束标记和返回文本能够完整解析。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=true`<br>`max_tokens=16` | 应支持 | 检查 chunk 结构、结束标记、文本拼接与 usage 末块。 |
| `deepseek0813_fim_stream_usage` | 流式 | 验证 SSE 分块可解析，并在结束前得到独立、算术一致的 usage 信息。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=true`<br>`max_tokens=16`<br>`stream_options.include_usage=true` | 应支持 | 检查 chunk 结构、结束标记、文本拼接与 usage 末块。 |
| `deepseek0813_fim_frequency_penalty_accepted_ignored` | 采样 | 验证采样参数 `frequency_penalty`、`max_tokens`、`prompt`、`response.choices[].text` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16`<br>`frequency_penalty=1` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_fim_presence_penalty_accepted_ignored` | 采样 | 验证采样参数 `max_tokens`、`presence_penalty`、`prompt`、`response.choices[].text` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`deepseek_v4_pro_0813_fim_beta` | `transport="fim_completions"`<br>`stream=false`<br>`max_tokens=16`<br>`presence_penalty=1` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_basic` | 基础能力 | 验证 `input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_instructions` | 基础能力 | 验证系统指令字段 `input`、`instructions`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage` 的协议位置和实际响应语义。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`instructions="You are a concise DeepSeek Responses API compatibility t...` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_stream` | 流式 | 验证 SSE 流式响应、结束标记和返回文本能够完整解析。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=true`<br>`max_output_tokens=32` | 应支持 | 检查 chunk 结构、结束标记、文本拼接与 usage 末块。 |
| `deepseek0813_responses_reasoning_low` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `input`、`max_output_tokens`、`reasoning.effort`、`response.output`、`response.output[].reasoning`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="low"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_responses_reasoning_none` | 负向/边界 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `input`、`max_output_tokens`、`reasoning.effort`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=64`<br>`reasoning.effort="none"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_responses_reasoning_minimal` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `input`、`max_output_tokens`、`reasoning.effort`、`response.output`、`response.output[].reasoning`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="minimal"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_responses_reasoning_medium` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `input`、`max_output_tokens`、`reasoning.effort`、`response.output`、`response.output[].reasoning`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="medium"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_responses_reasoning_high` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `input`、`max_output_tokens`、`reasoning.effort`、`response.output`、`response.output[].reasoning`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="high"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_responses_reasoning_xhigh` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `input`、`max_output_tokens`、`reasoning.effort`、`response.output`、`response.output[].reasoning`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="xhigh"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_responses_reasoning_max` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `input`、`max_output_tokens`、`reasoning.effort`、`response.output`、`response.output[].reasoning`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="max"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_responses_temperature` | 采样 | 验证采样参数 `input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`temperature` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=64`<br>`reasoning.effort="none"`<br>`temperature=2` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_temperature_thinking_accepted_ignored` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `input`、`max_output_tokens`、`reasoning.effort`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`temperature`。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="high"`<br>`temperature=2` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_responses_top_p` | 采样 | 验证采样参数 `input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`top_p` 的接受度；非思考模式下还检查返回值不是空壳。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=64`<br>`reasoning.effort="none"`<br>`top_p=1` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_top_p_thinking_accepted_ignored` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `input`、`max_output_tokens`、`reasoning.effort`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`top_p`。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="high"`<br>`top_p=1` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_responses_top_logprobs` | 基础能力 | 验证 `input`、`max_output_tokens`、`response.output`、`response.output[].content[].logprobs`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`top_logprobs` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=64`<br>`reasoning.effort="none"`<br>`top_logprobs=5` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_text_json` | 结构化输出 | 验证结构化输出参数 `input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`text.format`，并确认最终内容是可解析且符合约束的 JSON。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=192`<br>`reasoning.effort="none"`<br>`text.format.type="json_object"` | 应支持 | 内容必须能解析为 JSON；有 schema 时还要满足 schema。 |
| `deepseek0813_responses_text_verbosity_accepted_ignored` | 基础能力 | 验证 `input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`text.verbosity` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=64`<br>`reasoning.effort="none"`<br>`text.verbosity="low"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_user` | 基础能力 | 验证 `input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`user` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`user="loadtest_deepseek_responses_user"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_parallel_tool_calls_accepted_ignored` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`parallel_tool_calls=false` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek0813_responses_max_tool_calls_accepted_ignored` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`max_tool_calls=1` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek0813_responses_reasoning_summary_accepted_ignored` | 推理 | 验证指定推理开关/档位，并检查响应中的 reasoning/thinking 语义；涉及 `input`、`max_output_tokens`、`reasoning.effort`、`reasoning.summary`、`response.output`、`response.output[].reasoning`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="high"`<br>`reasoning.summary="auto"` | 应支持 | 检查请求档位和响应 reasoning/thinking 字段语义，不以可见文本长度代替。 |
| `deepseek0813_responses_store_false` | 基础能力 | 验证 `input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`store` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`store=false` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_store_false_accepted_ignored` | 基础能力 | 验证 `input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`store` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`store=false` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_store_true_accepted_ignored` | 基础能力 | 验证 `input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`store` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`store=true` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_previous_response_id_accepted_ignored` | 基础能力 | 验证 `input`、`max_output_tokens`、`previous_response_id`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`previous_response_id="resp_deepseek_ignored_previous"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_conversation_accepted_ignored` | 基础能力 | 验证 `conversation`、`input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`conversation.id="conv_deepseek_ignored"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_background_accepted_ignored` | 基础能力 | 验证 `background`、`input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`background=true` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_metadata_accepted_ignored` | 基础能力 | 验证 route 专属元数据 `input`、`max_output_tokens`、`metadata`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage` 放在正确的 body 或 header 位置。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`metadata.compatibility_probe="ignored"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_include_accepted_ignored` | 基础能力 | 验证 `include`、`input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`include=["reasoning.encrypted_content"]` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_prompt_accepted_ignored` | 基础能力 | 验证 `input`、`max_output_tokens`、`prompt`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`responses_prompt.id="pmpt_deepseek_ignored"`<br>`responses_prompt.version="1"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_truncation_accepted_ignored` | 基础能力 | 验证 `input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`truncation` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`truncation="auto"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_service_tier_accepted_ignored` | Route 元数据 | 验证 route 专属元数据 `input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`service_tier` 放在正确的 body 或 header 位置。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`service_tier="auto"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_safety_identifier_accepted_ignored` | 基础能力 | 验证 `input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`safety_identifier` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`safety_identifier="deepseek-responses-probe"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_prompt_cache_key_accepted_ignored` | 缓存参数 | 验证请求级缓存标识/缓存内容参数 `input`、`max_output_tokens`、`prompt_cache_key`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage` 能被正确接收和报告。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`prompt_cache_key="deepseek-responses-ignored-cache-key"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_prompt_cache_retention_accepted_ignored` | 缓存参数 | 验证请求级缓存标识/缓存内容参数 `input`、`max_output_tokens`、`prompt_cache_retention`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage` 能被正确接收和报告。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`prompt_cache_retention="24h"` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_context_management_accepted_ignored` | 基础能力 | 验证 `context_management`、`input`、`max_output_tokens`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage` 的请求兼容性以及对应响应字段是否正常。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=32`<br>`context_management=[1 items]` | 应支持 | 2xx 后仍需通过响应结构、内容、usage 和 returned-model 校验。 |
| `deepseek0813_responses_stream_options_accepted_ignored` | 流式 | 验证 SSE 流式响应、结束标记和返回文本能够完整解析。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=true`<br>`max_output_tokens=32`<br>`stream_options.include_obfuscation=false` | 应支持 | 检查 chunk 结构、结束标记、文本拼接与 usage 末块。 |
| `deepseek0813_responses_tools` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="none"`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`tool_choice="required"`<br>`multi_turn=true` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek0813_responses_tools_auto` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="none"`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`tool_choice="auto"`<br>`multi_turn=false` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek0813_responses_tools_named` | 工具调用 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="none"`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`tool_choice.type="function"`<br>`tool_choice.name="get_weather"`<br>`multi_turn=true` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek0813_responses_tool_choice_none` | 负向/边界 | 验证工具声明、tool choice、结构化调用参数以及必要时的工具结果回传。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=64`<br>`reasoning.effort="none"`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`tool_choice="none"`<br>`multi_turn=false` | 应支持 | 不能只看 HTTP 2xx；必须存在合法 tool/function call，follow-up 后还要有最终文本。 |
| `deepseek0813_responses_tools_required_thinking_reject` | 负向/边界 | 负向探针：发送文档不允许的 `input`、`max_output_tokens`、`reasoning.effort`、`response.output`、`response.parallel_tool_calls`、`response.previous_response_id`、`response.store`、`response.usage`、`tool_choice`、`tools`，确认网关明确拒绝而不是静默吞掉。<br>来源：`deepseek_v4_pro_0813_responses` | `transport="openai_responses"`<br>`stream=false`<br>`max_output_tokens=96`<br>`reasoning.effort="high"`<br>`tools_fixture="fixtures/tools_weather.json"`<br>`tool_choice="required"`<br>`multi_turn=false` | 应拒绝 | 应得到明确 400/422；若 2xx 则是 unexpected_acceptance。 |

## 去哪里看结果

- Web 控制台会展示当前 model/route/API Form 的 profile 状态和最近一次结果。
- 文字参数结果：`reports/param_tests/<provider>/<model>/verdict.json` 或 Web Job 目录。
- 图片参数结果：`reports/jobs/<job_id>/summary.json`、`plan.json` 和逐 case 文件。
- 总体解释方法见 [参数测试说明](../parameter_testing.md)，不要只看顶层 `pass`。
